{
  "name": "file_read",
  "description": "Read a text file inside the sandbox with optional line slicing.",
  "method": "POST",
  "endpoint": "/apifileread",
  "parameters": {
    "file": {
      "type": "string",
      "description": "Path relative to the sandbox root."
    },
    "start_line": {
      "type": "integer",
      "description": "Optional 1-based line to start from."
    },
    "end_line": {
      "type": "integer",
      "description": "Optional 1-based line to stop before."
    }
  },
  "required_parameters": [
    "file"
  ],
  "returns": "success flag and data.content containing the UTF-8 text."
}